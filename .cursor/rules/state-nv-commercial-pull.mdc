---
description: Nevada commercial project capture — read the playbook before any pull
globs: data/states/nv.json,data/raw/nv-*.json,scripts/pull-nv*.py,docs/states/nv.md
alwaysApply: false
---

# Nevada Commercial Pulls

Before capturing or refreshing Nevada, read **`docs/states/nv.md`** in full.

After the pull, update that playbook:

1. **What works** and **What failed last time**
2. **Pull log** with date, source, and outcome
3. The AUTO header stats (run `python3 scripts/sync-state-playbooks.py --state nv`)

Follow `.cursor/rules/state-data-pull-review.mdc` for the review checklist.

---
description: Alaska commercial project capture — read the playbook before any pull
globs: data/states/ak.json,data/raw/ak-*.json,scripts/pull-ak*.py,docs/states/ak.md
alwaysApply: false
---

# Alaska Commercial Pulls

Before capturing or refreshing Alaska, read **`docs/states/ak.md`** in full.

After the pull, update that playbook:

1. **What works** and **What failed last time**
2. **Pull log** with date, source, and outcome
3. The AUTO header stats (run `python3 scripts/sync-state-playbooks.py --state ak`)

Follow `.cursor/rules/state-data-pull-review.mdc` for the review checklist.

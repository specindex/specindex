# SpecIndex Roadmap

Canonical tracker — edit this file directly (git-tracked, no duplication problem).
A Google Sheet mirror is kept in Drive for non-technical sharing, refreshed on request
rather than on every edit (Sheets can't be updated in place — see note at bottom).

**Standing goal (2026-07-26): improving data coverage is the main, ongoing objective right now** — more states, more counties, deeper per-county detail. Proactively surface and recommend new/improved sources whenever working in this repo, don't wait to be asked. Verify every source live before building against it (see `docs/states/ga.md` → "Adding a new source: step-by-step"). Track deep-vs-thin county coverage at `specindex.ai/coverage/` (backed by `county_coverage`, see item 26).

**Sourcing priority order (2026-07-26):** national first, then state, then county. Capture broad national-level sources (SAM.gov, USAspending, DRI, other federal/nationwide feeds) to establish a baseline everywhere, then work state by state (state-level permit/bid boards), and only then spend effort on county-by-county local feeds (ArcGIS/Accela/EnerGov) — those are the highest-effort, most fragile integrations and should target the highest-value gaps shown in the Insights dashboard, not whatever research arrives first.

| # | Milestone | Status | Notes |
|---|---|---|---|
| 1 | Product strategy + architecture doc | Done | |
| 2 | Georgia corpus v0 (Kimi-structured), expanded to 126 projects / 10 states | Done | |
| 3 | Static SpecIndex site + project search UI | Done | |
| 4 | Firebase Hosting init + first deploy | Done | Live at specindex.ai |
| 5 | Custom domain specindex.ai | Done | |
| 6 | GitHub Actions CI/CD (PR previews + live deploy on merge to main) | Done | |
| 7 | Phase 1 Postgres (Cloud SQL) + read API (Cloud Run) | Done | Live and verified; schema v2 (migrations 002/003) adds numeric surrogate keys, external ID crosswalk, project_csi_divisions fact table |
| 8 | Complete corpus capture for remaining 40 states | In Progress | North Carolina built 2026-07-26 (see item 22). 38 states remain at baseline (SAM.gov bulk CSV only) |
| 9 | Wire the Next.js site to specindex-api instead of static JSON | Done | Live on specindex.ai as of 2026-07-25 |
| 10 | Spec book extraction pipeline (PyMuPDF -> CSI division LLM pass -> cited JSON) | In Progress | Built: `scripts/extract-spec-book.py`. Parsing/chunking verified against a synthetic PDF. `--pdf` now also accepts a `gs://bucket/path.pdf` URI (downloads and cleans up a temp file). `--write-bq` streams results into `warehouse.spec_extractions`/`citations`; verified 2026-07-25 with a synthetic payload queried back correctly. LLM classification pass itself still untested (no live Anthropic API key in this env) — see item 13 |
| 11 | Firestore or Postgres-backed authenticated manufacturer seats | Open | |
| 12 | Automated permit/press capture job + brand NER | Open | |
| 13 | Run extract-spec-book.py end-to-end against a real spec book PDF + live Anthropic API | Open | `ANTHROPIC_API_KEY` added and validated in `.env` 2026-07-25 (confirmed via `models.list()`); only blocker now is having an actual spec book PDF (not a permit form — none of the sample PDFs in `data/documents/` qualify). Deprioritized below #18 per 2026-07-25 decision: solidify the Georgia dataset first, since the extraction pipeline is only as valuable as the project corpus it attaches citations to |
| 14 | Wire api/main.py to expose schema v2 columns (project_sk / external_ids / csi_division_codes / status_code) | Done | Deployed 2026-07-25 (`specindex-api-00003-hrl`) — `project_sk`/`external_ids`/`record_type` now in `/v1/projects` response |
| 15 | Fix Chicago-style bundled-permit rows (60+ permits collapsed into one row's owner/architect/description) | Open | Data quality issue independent of schema — see `docs/DATA_SOURCES.md` |
| 16 | Pull SAM.gov GA construction data (federal solicitations + spec/drawing attachments) | Done | Solved 2026-07-25 by bypassing the quota entirely: SAM.gov publishes the full government-wide Contract Opportunities dataset as a public, no-key, no-quota CSV updated daily (`s3.amazonaws.com/falextracts/Contract Opportunities/datagov/ContractOpportunitiesFullCSV.csv`, ~232MB). New `scripts/pull-sam-gov-bulk-ga.py` filters it to 43 real, active GA construction opportunities (Savannah District design-build task orders, Fort Stewart, Fort Benning) — supersedes the old quota-limited `pull-sam-gov-ga.py`. Remaining gap: bulk file has no per-notice attachment/resourceLinks field, so spec/drawing PDFs for item 13 would still need a follow-up fetch of each notice page |
| 17 | GCS bucket (`specindex-ai-raw-documents`) + BigQuery `warehouse` dataset (`spec_extractions`/`citations`/`entity_resolution`) provisioned | Done | Smoke tested 2026-07-25 (`scripts/test-gcp-storage-bq.py`): GCS upload/download byte-for-byte round trip, BQ insert/query round trip. `entity_resolution` intentionally left unwired — no manufacturer-name canonicalization logic exists yet, so nothing should write raw (unresolved) mentions into a table meant for resolved ones. Note: BQ streaming inserts can't be `DELETE`d for up to ~90 min (streaming buffer), so ad hoc test rows may take a while to clean up |
| 18 | Solidify Georgia corpus (window + new sources + Accela) | In Progress | Georgia grew 489 → **1,394** projects (24mo window, up from 12mo), national corpus now 1,557 across 50 states. Added: Fulton County ArcGIS (299), Savannah/SAGIS ArcGIS (103), USAspending.gov federal awards (414), SAM.gov bulk CSV (43, no key/quota needed — see item 16) — see `docs/states/ga.md` 2026-07-25 update for full detail, source rejections (Columbus/Atlanta-CSV stale, Cobb/Gwinnett "ArcGIS" claims didn't hold up), and the Gwinnett Accela root-cause/fix. **Not done:** Atlanta/Cobb/Gwinnett Accela 24mo pulls failed — `aca-prod.accela.com` and GPR's `ssl.doas.state.ga.us` both started timing out simultaneously late in the session (likely self-inflicted rate-limiting from heavy automated traffic), which also wiped the working 318-record Gwinnett test via the shared-output-file race. Retry once backoff time has passed |
| 19 | Georgia Procurement Registry (GPR) — automate USG/state bid search | Open | Confirmed real, live, structured bid data (agency, category, NIGP codes, description, buyer contact, and a Documents tab likely holding real spec/bid attachments — directly relevant to item 13's extraction pipeline). Search UI (`govEntity`/`catType` dropdowns) loads inconsistently via Playwright `select_option`; next attempt should intercept the frontend's actual network requests to find the backend JSON endpoint rather than keep driving the UI blind. Also currently blocked by the same network timeout as item 18 |
| 20 | Fix `pull-ga-accela-commercial.py` output collision | Open | All three agencies (Atlanta/Gwinnett/Cobb) write to the same `data/raw/ga-accela-commercial.json` with no per-agency `--out` override — running any two concurrently or in sequence-with-failure silently clobbers prior results. Caused real data loss 2026-07-25 (lost a working 318-record Gwinnett pull). Add `--out` support or agency-specific default filenames before the next Accela run |
| 21 | Add Georgia Tech's ~8 named current capital projects to corpus (manual research) | Open | Found via `facilities.gatech.edu` "Current Major Projects": Tech Square Phase 3, Fanning Student-Athlete Performance Center, Curran Street Residence Hall, D.M. Smith Building renovation, East Campus Streetscape, Ferst Drive projects (x2), Chilled Water Distribution resiliency. Hand-curated list, not a bulk API — same tier as existing "prior research" entries. UGA's equivalent page not yet checked |
| 22 | Build North Carolina corpus (Mecklenburg + Wake) | Done | 2026-07-26: NC grew 195 → **5,126** projects (24mo). New `scripts/pull-nc-arcgis.py` covers Mecklenburg County (Charlotte, ArcGIS `permittype='Commercial'`) and Wake County (Raleigh + county-wide, ArcGIS `permitclassmapped='Non-Residential'`), both verified live before building. NC has no DRI equivalent (confirmed via research) and Accela is not the dominant platform there — only 1 of 17 candidate slugs worked (`CONCORD`). Full detail, endpoints, and bugs fixed in `docs/states/nc.md`. National corpus: 2,172 → **7,252** across 50 states |
| 23 | Fix `project_identity.dedupe_projects()` O(n² × merges) performance bug | Done | 2026-07-26: discovered via NC's pull — the old algorithm restarted its entire scan from index 0 on every merge found, so cost scaled with merge count, not just record count. Fine at Georgia's scale (~1,400 records, seconds) but NC's ~7,000 raw records (high near-duplicate rate, 1,925 real merges) ran 15+ minutes with no sign of finishing and had to be killed. New single-pass version verified as a non-regression against GA (identical output: 1,394 projects, 160 merges) — just 3s instead of minutes. NC itself: 68s for the same 1,925 merges |
| 24 | Batch Cloud SQL loader (`load-corpus-to-postgres.py`) | Done | 2026-07-26: loading grew past the point where one-row-at-a-time upserts were safe — a 7,252-row load through the Cloud SQL Auth Proxy ran 30+ minutes and then hit a server-side connection reset (idle/max-lifetime timeout) before finishing. Switched to `psycopg2.extras.execute_values` (batches of 500): same 7,252 rows load in 9.8s |
| 25 | Build Texas corpus (Dallas/Houston/Austin/San Antonio/Fort Worth) | Open | Started 2026-07-26, mostly unverified. TX has no DRI equivalent either (fully decentralized permitting, confirmed via research). Accela sweep: 0/3 slugs worked (`DALLAS`/`SANANTONIOSA`/`FORTWORTHTX` all 404). **Fort Worth confirmed live** (`mapit.fortworthtexas.gov/ags/rest/services/CIVIC/Permits/MapServer/0`, real schema incl. `JobValue`/`SqFt`/`Owner_Full_Name`) but not yet built into a script. Dallas/Austin/San Antonio/Houston leads found via search (Socrata resource IDs, ArcGIS Hub portals) but none verified live yet — first freshness-check attempts returned empty/error responses, need retrying with correct query syntax before trusting any of them. Do not build against unverified URLs |
| 26 | `county_coverage` table + `/coverage` page | Done | 2026-07-26: new Cloud SQL table (`db/migrations/004_county_coverage.sql`) populated by `scripts/compute-county-coverage.py` (derives source attribution from `project_id` prefixes, classifies each county deep/thin), backing a new `/v1/coverage` API endpoint and `specindex.ai/coverage/` page (searchable/sortable, not linked from public nav — internal tool). This is now the primary dashboard for directing coverage-expansion work — see the standing goal at the top of this file |
| 27 | Forsyth County GA vs NC — resolved a real cross-source contradiction | Done | 2026-07-26: two pieces of secondhand research disagreed about which state `geo.forsythco.com/gis3/rest/services/Public_EnerGovPlans/Building_Permits/FeatureServer/0` belongs to (one claimed GA, one claimed NC, same URL). Settled with real coordinates (`outSR=4326` on a sample query): 34.15-34.31°N, 84.0-84.2°W — confirmed **Forsyth County, GA** (Cumming, north Atlanta). This URL must NOT be used for NC's Forsyth County (Winston-Salem, ~36.1°N) — that's a different, not-yet-found source. Not yet built into a GA pull script (244 commercial permits/90d claimed, unverified count) |
| 28 | 3-agent coverage strategy designed (Puller / Depth / Quality) | Done | `docs/AGENT_STRATEGY.md` — national-before-state-before-county build order, one periodic pipeline instead of ad hoc scripts |
| 29 | `state_quality` table + Quality tab on `/coverage` | Done | Per-state field completeness (city/value/contractor/date) + freshness, `/v1/quality` endpoint |
| 30 | Week-over-week deltas on `county_coverage` + Insights tab | Done | `previous_project_count`/`delta` columns; Insights tab shows a "Since last run" column per state |
| 31 | Bucket `dedupe_projects()` by (state, county) | Done | `same_project()` already rejects cross-county pairs immediately; bucketing skips those comparisons entirely. Verified byte-identical to the prior algorithm on GA; running it for real on NC surfaced one genuine pre-existing near-duplicate (5126→5125) |
| 32 | Scheduled national + state pullers + weekly/monthly health email | Done | `.github/workflows/pull-national.yml` (weekly) and `pull-state.yml` (monthly, GA DRI + NC ArcGIS) — pull → merge → load → coverage → quality → email, authenticated via Workload Identity Federation (no stored key). First live runs verified green end-to-end |
| 33 | Fixed GitHub repo default branch (was a feature branch, not `main`) | Done | `gh workflow run`/fresh clones now correctly default to `main` |
| 34 | MLS-permanence audit + fix: deterministic merge IDs | Done | `project_sk` (Postgres IDENTITY, permanent once assigned) is only as stable as `project_id`, and `prefer_canonical()` picked the surviving id by a completeness score that could change between rebuilds — a real bug where the "permanent" id could silently drift. Fixed via `_stable_canonical_id()`: DRI preferred, else lexicographically smaller wins, independent of completeness. Verified via a simulated completeness-flip test (same id both times, would not have been before) |
| 35 | Socrata adapter (`scripts/socrata_adapter.py`) | Done | Shared SODA API query helper for Socrata-backed cities (NYC/LA/Chicago/Seattle/New Orleans), mirroring the ArcGIS `query_layer()` pattern. No per-city pull script wired up yet — pending verification |
| 36 | Internal ops dashboard (`specindex.ai/ops/`) | Done | Pipeline run history (per-step outcomes, project-count deltas), live API/DB-pool health, data-staleness watchlist, known infra costs. Client-side fetched (not build-time baked) since freshness matters more than SEO here |
| 37 | `project_sources` + `project_events` tables; real lat/lon/zip populated | Done | Formalizes the existing `sources` JSONB array into structured provenance (migration 008); new timeline capability derived at load time from `project_id`/`record_type` (migration 009). NC's ArcGIS pullers were querying with `returnGeometry=false`, discarding real coordinates — now populated (~718 rows). Found and fixed a related bug: `merge_projects()` had no fallback-fill logic for lat/lon/zip, so an old (no-coords) record winning as merge primary silently discarded a fresh record's real coordinates |
| 38 | Admin map (`specindex.ai/map/`) | Done | Mapbox GL JS (internal-tool choice; Leaflet+OSM reserved for any future user-facing map), clustered pins, county/city filters (zip filtering deferred — no source has broad zip coverage yet). Token issuance gated through `/v1/ops/mapbox-token`, a Postgres-backed monthly quota (not in-memory — an in-memory counter would reset on every Cloud Run cold start) |
| 39 | `spx_id` branded identifier | Done | The one identifier customers should ever see (`SPX-000123`, wrapping the permanent `project_sk`) — replaces the raw `project_id` slug everywhere it was shown as a labeled field. `project_id`/`project_sk` stay internal plumbing (URL routing / DB key respectively) |
| 40 | News enrichment (`project_news` table, `scripts/enrich-news.py`) | Done | Google News RSS, verified live — real results for high-profile projects, but noisy for generic permit names (filtered by requiring city/county/state to appear in the result + a junk-title denylist). Scoped to the highest-value, not-yet-checked projects per run (default 50), tracked via `project_news_checks` |
| 41 | County ArcGIS + state EDA source research (verification pass, not yet built) | Open | 5 solid new county sources verified live/fresh/correctly-geolocated: Durham NC, Maricopa AZ, Fort Worth TX, Denver CO, Nashville/Davidson TN. State economic-development databases: NY (Socrata, quarterly, rich fields) and AL (annual PDF, full project table) are the strongest of 10 checked; AZ and WA don't actually have a public project-level database despite secondhand claims. None built into pull scripts yet |
| 42 | Project page redesign (Amazon-listing style + project score) | Open | See `docs/PROJECT_PAGE_REDESIGN.md` — timeline/provenance/map sections are buildable now (items 37-38 already provide the data); the project score (value + recency + news, the core monetization hook) and its exact formula are still open, to be iterated on directly rather than invented unilaterally |
| 43 | GitHub Actions `pull_request`-triggered checks got stuck mid-session | Known issue | For roughly an hour, pushes to the open PR's branch stopped triggering "Deploy to Firebase Hosting on PR" entirely (zero check-suites created) — ruled out rate limits, Actions minutes (repo is public, unlimited), YAML errors, and a GitHub-wide outage. `workflow_dispatch` and direct `push` events kept firing normally throughout, so this was isolated to `pull_request` `synchronize` webhook delivery specifically. Workaround used: push directly to `main` (no branch protection blocks this) instead of relying on the PR merge button — GitHub auto-detected the content landed on `main` and marked the PR merged. Revisit if it recurs |
| 44 | CI deploy started failing at 27K+ pages: default 60s static-page timeout | Done | 2026-07-26: `Deploy to Firebase Hosting on merge` failed on commit `2087fda` — GitHub's shared runners are slower/more contended than local hardware, and individual `/projects/[id]/` page fetches were exceeding Next's default 60s `staticPageGenerationTimeout` there (though not locally), failing the whole build after 3 retries. Bumped to 180s in `next.config.ts`. Notable: this is the same scaling wall flagged in item 42's deferred SSR-migration plan, arriving sooner than the "revisit at 100,000 projects" estimate — worth watching, not yet worth reopening that migration |
| 45 | Pricing page — kept static for now | Open | Explicit 2026-07-26 decision: leave the pricing page as-is rather than rebuild now. Revisit together with item 47's "pricing needs real numbers" finding (ballpark $/month per territory) once ready to commit to actual figures |
| 46 | Projects dashboard redesign: sales-intelligence reframe (territory + category + score, not a database browser) | Phase A done | Plan at `~/.claude/plans/curious-coalescing-frog.md`. **Phase A (no accounts) — shipped 2026-07-26:** default `/projects` view leads with "Top 50 in your territory" framing (score-sorted, page 1), territory+category persisted to `localStorage` with a first-visit onboarding nudge; "🔔 N new this week" toggle/badge (fixed a real bug along the way — `first_seen_at` had no column default and was NULL on every project loaded since migration 002, see migration 014); List/Map toggle on `/projects` itself via new public `/v1/projects/map-points` (Leaflet+OSM, bounded to the active filter — distinct from the Mapbox-GL, unbounded, ops-only `/v1/ops/map-points`). **Phase B (needs real user accounts — nothing like this exists in the app today):** Firebase Auth, server-persisted saved territory/searches, weekly "N new projects in your territory" digest email via existing Gmail SMTP creds. Division/CSI filter explicitly NOT part of this — see item 41's finding that `project_csi_divisions` is 100% empty |
| 47 | UX feedback audit — general product-manufacturer-POV review | Open, deferred | Full doc from 2026-07-26. Key findings not yet covered by item 46: "Request Demo" is a raw `mailto:` link (no inline form/modal — kills leads); no self-serve evaluation path before a demo request; demo form buried below the fold (add a sticky/inline mid-page offer); pricing page has no real numbers (see item 45); weak generic testimonials — recommend division-specific social proof (e.g. quote from "VP Sales, Division 08" next to that CSI row); "In Development" features (spec-book extraction, share-of-spec) are the real differentiators and should have a waitlist/early-access CTA; division-specific landing pages (`/divisions/hvac`) don't exist — all 19 divisions share one homepage; homepage should let a visitor pick state(s)+division and see a relevant number ("847 GA/TN/Carolinas projects needing roofing") before any signup |
| 48 | `/projects/` page audit — detailed bug/UX list | Open, deferred | Full doc from 2026-07-26, written against a build that predates this session's pagination/search rewrite (item... see `ProjectsDashboard.tsx`) — several findings (search not filtering, "20084 of 20084" never changing, no pagination) are **already fixed** and stale as written; re-verify against current prod before assuming still broken. Findings that likely still hold: county dropdown not scoped to selected state; filters don't update the URL (no shareable/bookmarkable filtered view, back/forward doesn't restore state); no "Clear all filters" control; "All product categorie" truncated dropdown label; inconsistent money formatting across cards ($100,118 vs $1.9M, pick one style); raw/uncleaned permit-string titles overflowing mid-word; no search icon/loading state/empty-state; ambiguous "Not reported" field (unlabeled which field); unexplained `SPX-######` ID on cards; hero stat numbers same font-weight as labels. Missing features called out here duplicate item 46 (saved searches/alerts, CSI division tags, territory filter) — build once, there |
| 49 | SEO/analytics/growth infra rollout (Gemini-sourced roadmap, targeting 1M+ project pages) | Open | Full doc from 2026-07-26. **Correction to the doc's framing:** it's written assuming ISR (`revalidate`), but the site is `output: "export"` (pure static, see item 42/44) — ISR needs a real Node server and isn't compatible with static export as-is. Only one item actually requires it (flagged below); everything else works fine build-time on the current architecture. **P0, no conflict:** sitemap index split (`generateSitemaps()`, cap 50K URLs/file — relevant well before 1M pages given today's 27K already stresses one `sitemap.xml`); `metadataBase` + `title.template` in `app/layout.tsx` + explicit canonical in `generateMetadata()`; GA4/GTM via `@next/third-parties/google` (static/client-side, zero conflict). **P1, done 2026-07-30:** `/projects/[state]/[trade]/` pSEO hub pages (`app/projects/[state]/[trade]/page.tsx`) — `generateStaticParams()` over the real (state × CSI division) combinations that have coverage (skips empty combos rather than emitting thin pages), built from a per-state sample via `lib/projects.ts`'s new `getStateSample()`/`getStates()`, filtered with `lib/divisions.ts`'s new `getProjectsForDivision()`; wired into `app/sitemap.ts`. Dynamic OG images (`app/projects/[id]/opengraph-image.tsx`, `next/og`'s `ImageResponse`) — same `generateStaticParams()` as the project detail page, degrades cleanly on the public teaser's missing fields (value/score) since it runs at build time with no signed-in session, same as the page itself post-item-49.5's auth gate. **P1, blocked on a real decision, not code:** DB connection pooling (PgBouncer or Cloud SQL's built-in pooler) — checked live 2026-07-30: this instance is Cloud SQL **Enterprise** edition; GCP's managed connection pooling requires **Enterprise Plus** (a real edition upgrade, cost + migration implications). The alternative is standing up a standalone PgBouncer service — genuinely new infrastructure (new Cloud Run service or sidecar, networking). Neither is a code change Claude should make unprompted; needs Asif's call on which path (if either) before building. | 
| 49.5 | Project data endpoints required no auth despite the `/projects` Clerk sign-in wall (UI-only gate) | Done | Found during a page audit 2026-07-29/30: `curl`ing `/v1/projects` directly returned the full corpus, no token needed — anyone could bypass the sign-in modal entirely. Fixed in PR #70 (merged 2026-07-30): `/v1/projects` and `/v1/projects/map-points` now require a Clerk session or `SPECINDEX_BUILD_TOKEN` (build-time-only shared secret, `next build` has no user to attach); `/v1/projects/{id}` returns a trimmed public teaser unless the caller has a real signed-in session, so the ~200 statically pre-rendered SEO pages stop baking full records into public HTML; `/visibility` no longer fetches its 2,000-row sample server-side and serializes it as a raw prop — now fetches client-side behind `ProjectsGate`, same pattern as `/projects`. Verified live post-deploy: anonymous `/v1/projects` → 401, build-token → 200, anonymous `/v1/projects/{id}` → trimmed teaser with `"gated": true` |
| 50 | Per-project SEO: title/meta/canonical/JSON-LD template + AWS New Carlisle worked example | Open, flagged | Full doc from 2026-07-26. **Real problem found:** the doc's own worked example claims "$11.0B Phase I / $26.0B total," "2.2 GW power envelope," and "AWS Trainium2 / Anthropic Claude" workload for the AWS New Carlisle page. Checked against our actual sourced data: `estimated_value_usd` is **$13.8B** (cited, verified), and no power-envelope figure or Trainium2/Claude claim exists anywhere in our sources. These aren't a refinement of our numbers, they're different, unsourced numbers about a real company's real project — do not inject them into JSON-LD or page copy as fact. Canonical URL enforcement and a structural JSON-LD block built **only from our own verified DB fields** (name, address, real `estimated_value_usd`) are fine and overlap with item 49's P0 SEO work; the breadcrumb trail (`Home → Projects → Indiana → Electrical & Data Centers`) depends on pSEO hub pages that don't exist yet (see item 41/46 CSI-data non-goal) |
| 51 | Priority score v2: multi-signal matrix (Fit × Timing × Actionability × Competition), behavioral feedback loops, spec/addenda AI parsing | Open, future | Full doc from 2026-07-26. Good north-star direction, not actionable yet — most of it needs data we don't source: contractor capacity/sweet-spot fit needs a contractor profile (doesn't exist, needs accounts — item 46 Phase B); bidder-density needs bid-count data we don't collect; "verified direct GC/architect contacts" needs contact data we don't have; behavioral feedback loops (saves/dismissals boosting future scores) need accounts + per-user history. The one piece already shipped: score **explainability** — `ProjectScoreBadge.tsx` already shows the value/recency/news breakdown per project, matching this doc's "why 94/100" ask. Revisit this as v2 of the score once Phase B (accounts) exists to supply the missing signals |
| 52 | Project detail page UI: score badge with per-pillar progress bars, sticky action bar, CSI division chips | Open | Full doc from 2026-07-26. CSI division chips blocked on empty `project_csi_divisions` data (item 41/46). "Claim Territory Lead" / workflow CTAs need accounts (item 46 Phase B). The sticky action bar (title + score visible while scrolling a long write-up) is small, doable, no-account, no-data-blocker UI polish — candidate for a future cheap-wins pass |
| 53 | LLM-generated "Opportunity Analysis" write-up template for project pages | Open, flagged | Full doc from 2026-07-26. Same fabrication risk as item 50 — the template asks an LLM to "perform a targeted search... to compile" investment/timeline/stakeholder facts about a real project. If this gets built, it must be constrained to fields already in our verified DB record (never invented), with a human review step before publishing anything framed as fact about a named real company. Not safe to fully automate given the demonstrated mismatch in item 50 |
| 54 | Geocode-missing fallback architecture: `geocodeStatus` enum, regional-boundary fallback card, async backfill worker, on-demand revalidation | Open, partial | Full doc from 2026-07-26. The `revalidateTag`/on-demand-ISR piece conflicts with static export, same as item 49's P3. Everything else is real and doable without that: `ProjectLocationMap.tsx` **already** shows a "Location not yet geocoded" fallback instead of a blank map when lat/lon is null (built earlier this session) — the doc's "Regional Boundary Card" (city/county/state + centroid) is a nicer version of that, worth building. A `geocodeStatus` column (PENDING/CITY_FALLBACK/EXACT/FAILED) would be a real improvement over today's bare null-lat/lon-means-ungeocoded state, and the async backfill-worker part just needs a normal build-and-redeploy trigger instead of `revalidateTag` |
| 55 | AI data-enrichment pipeline via Vercel AI SDK (`@ai-sdk/google` + `@ai-sdk/anthropic`, two-tier Gemini/Claude, Zod schemas) | Open, flagged | Full doc from 2026-07-26 (two versions: a generic Next.js API-route pipeline, and a GA-corpus-specific `scripts/enrich-ga-corpus.ts`). **Stack-fork concern:** the existing enrichment/scoring pipeline is Python (`scripts/compute-project-scores.py`, `scripts/extract-spec-book.py` — the latter already calls the Anthropic SDK directly, `ANTHROPIC_API_KEY` already validated, see item 13). Standing up a parallel Node/TypeScript AI pipeline for the same job would mean maintaining two enrichment systems in two languages. If LLM-based field enrichment is wanted, extend the existing Python pipeline rather than fork into Vercel AI SDK. Separately, `app/api/cron/enrich-projects/route.ts` presumes a Next.js server route, which the static export doesn't have — another instance of item 49's ISR conflict |
| 56 | 50-state source-discovery prompt template (5-tier hierarchy, adapter classification, generate `{STATE}.md` playbooks via LLM research) | Open, flagged | Full doc from 2026-07-26. Directly conflicts with this session's hard-won, repeatedly-necessary discipline: **verify every source live before building against it** (see the standing goal at the top of this file). Concrete cost of skipping that, this session alone: Durham's field categories, Nashville's date field, Fort Worth's `SqFt` type, and AZ/WA's claimed-but-nonexistent public databases were all wrong in secondhand research until checked against live endpoints (items 22/25/41). This template proposes generating all 50 state playbooks from an LLM's "comprehensive discovery pass" — i.e. the exact failure mode already paid for multiple times. Keep expanding state-by-state the way item 25 (Texas) is already doing: research leads, then verify each endpoint live before writing a puller against it |
| 57 | Homepage/footer UI redesign: 4-column footer (state hubs, CSI divisions, methodology, company), hero score-card preview, "12 projects to chase" headline | Open | Full doc from 2026-07-26. Reasonable direction overall — footer link grid, sharper hero copy, and a visual score-pillar explainer are all doable now with no data blocker. Two things flagged: (1) the "Source & Citation Callout Box" on project detail pages (`📄 Source Citation: Fulton County ArcGIS Permit #...`) directly contradicts your explicit earlier instruction ("Sources us only for news articles don't give the source for the project info") — `ProjectProvenance.tsx` was deliberately deleted for this reason, don't reintroduce it; (2) CSI-division footer links and a `/vs-dodge` comparison page both need things that don't exist yet (CSI data; real competitive research), not just a UI slot |
| 58 | GCP infra modernization: AlloyDB, Vertex AI (vs. AI Studio), Cloud Tasks, Firebase App Hosting, PgBouncer, IAM DB auth, budget alerts | Open, mostly premature | Full doc from 2026-07-26. Biggest item to flag: Firebase App Hosting + on-demand ISR is the same deferred SSR migration (item 42, "revisit at 100,000 projects") under a different GCP product name — don't reopen it via this door either. AlloyDB is explicitly pitched in the doc itself as relevant ">500k pages"; we're at ~27K. Vertex AI vs. AI Studio is a reasonable swap **if/when** item 55's enrichment work actually ships, not urgent standalone. Two pieces are genuinely useful **right now**, independent of everything else here: PgBouncer/connection pooling (already flagged in item 49 as directly relevant to the real CI timeout root-caused in item 44) and GCP budget alerts (cheap, no conflict, catches a runaway LLM loop or accidental Cloud SQL scale-up before it's expensive) |
| 59 | `merge-national-corpus.py` dedup regressed to expensive-path comparisons for every source added this session | Done | 2026-07-27: a `merge-national-corpus.py` run hung for **108+ minutes** (previously ~1-3 min) after this session's 17-state batch (~135K raw records added). Root cause found by reading `same_project()`, not just inferred from scale: `MUNI_ID_PREFIXES` in `scripts/project_identity.py` (the cheap-exit list — "both records have their own unique per-record ID, so they're different projects without an expensive fuzzy-name check") **still only had the 6 original Georgia prefixes from early in the project**. Every new pull script this session (NC/AZ/TX/CO/TN's `pull-county-arcgis.py` sources, plus all 17 states added today) was silently falling through to the expensive path — `SequenceMatcher`-based fuzzy string comparison on every single pair within a bucket, not the O(1) exact-ID check the design intends. Cook County IL alone has ~22,461 records in one bucket; at the expensive path that's ~504M non-trivial string comparisons. Fixed by adding every real per-record-ID source prefix used this session to `MUNI_ID_PREFIXES` (`il-cook-`, `nj-dca-`, `pa-philadelphia-`, `md-montgomery-`, `mi-detroit-`, `va-fairfax-`, `wa-seattle-`, `fl-miamidade-`, `ct-hartford-`, `vt-burlington-`, `ri-providence-`, `de-statewide-`, `ks-overlandpark-`, `ar-bentonville-`, `nd-fargo-`, `wi-milwaukee-`, `ny-nyc-`, plus the pre-existing `nc-*`/`az-maricopa-`/`tx-*`/`co-denver-`/`tn-*` sources that had the same silent gap) and adding a `MUNI_ID_INFIXES` check for the federal `-sam-`/`-usaspending-` per-award IDs that recur across every state prefix. **Process going forward: every new `pull-*.py` script must add its ID prefix to `MUNI_ID_PREFIXES` when it's built, not as an afterthought** — this is exactly the kind of gap that's invisible until a bucket crosses ~10-20K records and stays invisible before that |
| 60 | Switch NJ DCA pipeline's Sonnet dedupe node from direct Anthropic API to Claude-on-Vertex | Open, blocked on GCP quota | 2026-07-27: `sonnet_backend=vertex` is already coded (`model_b_sonnet.py`'s `_call_claude_vertex`, using the same Workload Identity Federation auth already wired into GitHub Actions for Cloud SQL — no separate secret needed). Live-tested against `specindex-ai`'s Vertex project and it reaches the model (not a permissions/enablement problem), but hit a hard `429 RESOURCE_EXHAUSTED`: `Quota exceeded for aiplatform.googleapis.com/global_online_prediction_requests_per_base_model with base model: anthropic-claude-sonnet` — the project's Claude-on-Vertex quota is effectively zero. Needs a manual quota increase request in the GCP console, approval timeline uncertain. **ACTION NEEDED (Asif):** console.cloud.google.com → IAM & Admin → Quotas → filter by service `aiplatform.googleapis.com`, search `anthropic-claude-sonnet` → select it → "Edit Quotas" to request an increase. Confirmed 2026-07-28: this can't be done via `gcloud`/API on my end -- the quota shows no set numeric limit to increase from, meaning Anthropic-on-Vertex access is gated behind Google's manual review, not a self-service call. Shipped the daily cron (`pull-nj-dca-pipeline.yml`) on the proven direct-Anthropic-API path (`ANTHROPIC_API_KEY` repo secret) in the meantime so ingestion isn't blocked on this. **Once quota is approved:** flip `NJ_DCA_SONNET_BACKEND=vertex` in the workflow env, verify a live run, then drop `ANTHROPIC_API_KEY` from repo secrets — consolidates billing/auth into GCP only, matching the stated goal of running everything through GitHub + GCP with minimal separate credentials |
| 61 | Automated cost cutoff for Vertex AI spend, scoped to just that API | Open, deferred | 2026-07-27: set a $100/month GCP budget alert on `specindex-ai` (50/90/100% thresholds) plus Anthropic's own hard usage limit (real cutoff — API calls fail once hit, unlike GCP budgets which are notification-only). Considered wiring the GCP budget's Pub/Sub notification to a Cloud Function that disables billing automatically at 100%, but that pattern kills the whole project — Cloud SQL and the live site along with the pipeline — a worse outcome than the overspend it prevents. Once item 60 (Vertex Claude) ships, the safer targeted option is a **Vertex AI quota** (max requests/day on `aiplatform.googleapis.com`) that throttles just that API without touching Cloud SQL or anything else. Not built yet — do this alongside item 60, not before, since there's no Vertex spend to cap until that switch happens |
| 62 | **ACTION NEEDED (Asif):** Sign up for a Gwinnett County Accela Citizen Access account | Open, blocked on you | 2026-07-28: Gwinnett County's permit portal (`aca-prod.accela.com/GWINNETT`) has a real, public, no-login "General Search" for permits (by permit number, address, project name, parcel, contractor/license), but no visible date-range filter on the anonymous view — logged-in accounts on Accela deployments commonly unlock broader search/filter/export capability (e.g. date-range, bulk results) than the public form exposes. Register at `https://aca-prod.accela.com/GWINNETT/CommunityView/account/new` so the scraper (item 63-in-progress: Playwright + Gemini Flash extraction) can log in and pull a real incremental date-range query instead of being limited to one-off lookups. Same Accela platform likely applies to other GA counties found in the research pass (Cobb uses an Accela dashboard too) — one account may not cover multiple counties, check per-county |
| 63 | Build a Tyler EnerGov Playwright scraper (new provider type, like `accela_provider.py`) | Done, El Monte/Glendale/Alhambra/Carson proven; Pomona blocked | 2026-07-28: built `core/ingestion/energov_provider.py`. Live investigation found the search API (`POST .../apps/selfservice/api/energov/search/search`) validates something about the request that only a genuine Angular-originated click satisfies — a hand-crafted POST replaying the exact captured headers/body, even via Playwright's session-bound `page.request` (real cookies included), still 500s. Root cause not resolved; worked around by driving the real UI (click "Search") and capturing the resulting JSON response instead of scraping HTML — more reliable than Accela's table-scrape once it works. **Real limitation found on El Monte:** the "Advanced" panel (record-type + date-range filters) stays `ng-hide`d in every UI state tried, so there's no confirmed way to set a server-side date filter — the blank/default search returns a fixed-ish sample of real commercial permits, not the newest records, regardless of when it's run. Shipped as a **backfill source** (`lookback_days=900`, real data) rather than a rolling feed until that's solved. **Second real bug found generalizing to Glendale:** the response-wait was a fixed timeout (3s for the initial search, 2.5s per pagination click) tuned to El Monte's speed; Glendale's SPA fires the same request ~6s+ later, so it silently returned 0 rows on the first attempt, not an error. Fixed by polling for the captured response (up to 10s) instead of guessing a fixed wait. Verified: El Monte (7 merged) + Glendale (2 merged) both working end-to-end. **Alhambra + Carson added 2026-07-28** (both turned out to be `tylerhost.net`-hosted, not self-hosted as originally assumed): Alhambra worked immediately (+2 merged). Carson's default `SearchModule` dropdown was scoped to "License" (returning only business-license records for a blank search, not permits) — fixed by explicitly selecting "Permit" and dispatching a real `change` event (Playwright's `select_option()` alone doesn't fire Angular's ngModel binding); this fix is generalized (safe no-op on tenants without the selector or already defaulting to Permit), not Carson-specific. Also found and fixed a crash: an intermittent response with `Result: null` (likely a side effect of the module-change firing its own incomplete auto-search) was throwing an unhandled `AttributeError` in the response handler — now explicitly ignored so it doesn't corrupt the poll loop. Carson still shows genuine intermittent flakiness beyond that fix (~1 in 3 runs gets no valid response at all, even after adding one bounded retry-click) — real platform behavior on this specific tenant, not a bug worth chasing further right now. Carson +4 merged; CA total now 1,985 real projects. **Pomona blocked:** the only genuinely self-hosted tenant found (`connect.pomonaca.gov/energov_prod/selfservice`, not tylerhost.net) — confirmed reachable (HTTP 200) but its own infra is much slower (~49s page load vs ~3-6s for the tylerhost.net tenants) and no search response was ever captured even after a 90s `networkidle` wait + explicit selector wait + force-click through a persistent "Processing" overlay. Config kept in state_configs.py as `_CA_POMONA_ENERGOV_CONFIG` (underscore-prefixed, not registered in `STATE_CONFIGS`) for a future attempt with a much larger timeout budget. |
| 64 | Smaller LA County backlog: Norwalk (EDGEsoft PIP, address-enumeration puller), Burbank (monthly PDF permit reports), Lakewood (OpenGov/ViewpointCloud, Auth0-gated), Inglewood/Burbank PDF backup path | Open, lower priority than #63 | 2026-07-28: real, live, jurisdiction-confirmed sources, each needing a different one-off integration smaller than a full EnerGov scraper. Norwalk is the cheapest (plain HTTP GET returns server-rendered HTML tables, no Playwright needed at all — just needs an address list to enumerate against, e.g. seeded from a parcel FeatureServer). Burbank's PDFs are genuinely rich (Disney, Southwest Airlines, Hollywood Burbank Airport permits) but need a PDF-table parser plus a way past Cloudflare's JS challenge on the listing page (the PDFs themselves aren't challenge-protected — direct doc URLs work). Lakewood is the most expensive (Auth0 bearer-token capture via Playwright). Compton (CitizenServe, "I am human" gate) and South Gate (EdgeSoft, permit-level-only access) are explicitly **not** on this list — see `[[feedback_bash_auto_allow]]` sibling note: declined to build CAPTCHA/anti-bot-evasion tooling (residential proxies, TLS fingerprint spoofing, solver APIs) for Compton even though asked about it, since that class of tooling is out of scope regardless of legitimate end use; the real unblock there is contacting the city directly |
| 65 | Move downloaded source documents (RFPs, EIS/EDA packets) out of git into GCS | Done | 2026-07-28: `data/documents/` was accumulating large PDFs (124MB for NJ alone) with no LFS rule covering it. Uploaded the existing NJ set (69 files) to `gs://specindex-ai-raw-documents/new-jersey/` (bucket already existed, unused until now — mirrors local per-project folder structure incl. `manifest.json`). Added `data/documents/` to `.gitignore`. `scripts/fetch-nj-documents.py` still writes locally first; not yet updated to upload directly to GCS — next state's document pipeline should either add that or this becomes a manual step each time |
| 66 | Deterministic (no-LLM) field-mapping path for clean ArcGIS/Socrata sources + promote 6 counties verified by the `asif-test` sandbox | Done | 2026-07-28: Asif ran a separate local sandbox (`~/Projects/asif-test`, not in this repo, no remote) testing deterministic-only source discovery across all 3,144 US counties by population -- found 10 counties with a working feed (0.3% of counties; validates the existing "deterministic → Flash → Sonnet, never skip" rule and the 50-state cost-decision doc, item... see `docs/STATE_CAPTURE_RECOMMENDATION_PATH.md`). Asif fed Recommendations one at a time for: Wayne/MI (new), Cook/IL (new), Miami-Dade/FL (new), King/WA (new), LA City + LA County/CA (already existed here, re-verified), Maricopa/AZ (already existed, re-verified). Each recommendation explicitly said "do not run Flash" since the source data is already clean/structured -- specindex didn't have that no-LLM path for generic `arcgis`/`socrata` provider types (only `sam_gov`/`usaspending`/`accela`/`energov` were in `NO_LLM_PROVIDER_TYPES`), so built `core/ingestion/generic_mapping.py` (ported from asif-test's proven `rows_to_projects()`) as an opt-in `to_projects()` on both `ArcGISProvider` and `SocrataProvider`, gated on a state config setting `name_fields`/`address_fields` -- states that don't set these keep going through Flash/Sonnet exactly as before, so this doesn't change behavior for any existing state. `pipeline.py` routes to the no-LLM path when either the provider_type is in `NO_LLM_PROVIDER_TYPES` or `name_fields` is set. Every new source verified live before wiring (Cook County's `date_issued` field had a real data-quality bug -- `MAX()` returned year 2210 from 4 garbage rows out of 92,437 -- caught by direct query, not trusted from the recommendation, same discipline as the TX Collin bad-future-timestamp trap; Wayne/Miami-Dade/King all matched their quoted row-count estimates closely). **Real bug caught and fixed post-merge:** a retroactive id-cleanup script (fixing a cosmetic double-state-prefix in generated ids, e.g. `ca-ca-losangeles-ladbs-...`) bypassed `merge_into_state()`'s proper dedup and created 867 exact-id collisions in `fl.json` + 2 in `az.json` against richer pre-existing records (LLM-extracted `owner`/`square_footage` populated vs the new deterministic pull's blanks) -- caught by an explicit id-uniqueness check before committing, fixed by keeping the richer pre-existing record and dropping the newly-added duplicate. Lesson: any script that directly rewrites `data/states/*.json` outside `merge_into_state()` must re-verify id uniqueness afterward, not just before. Net new records merged: CA +501 (LA City Socrata +471, LA County ArcGIS +30), MI +50 (Wayne), IL +17 (Cook), FL +741 net (Miami-Dade, after removing the 867 dupes), WA +108 (King), AZ +0 (Maricopa's 2 candidate rows were both already captured by an earlier richer extraction). |
| 67 | Continue promoting `asif-test` recommendations: 7 more counties + first CARTO source; San Diego probed and ruled out | Done | 2026-07-28, same session as item 66. Verified live and merged: Tarrant/TX (+366), Franklin/OH (+550), Cuyahoga/OH (+249), Mecklenburg/NC (+21), Wake/NC (+52), Fairfax/VA (+66), a second Williamson/TX feed (`TX-WILLIAMSON-PERMITS`, +58 -- different org/dataset than the existing `TX-WILLIAMSON` Site_Development feed, kept as a separate state key rather than merged). All seven fetch counts matched the recommendation's estimates exactly once probed. Built `core/ingestion/carto_provider.py` (first new platform type since EnerGov/CKAN) for Philadelphia/PA (CARTO SQL-over-HTTP API, `phl.carto.com`) -- verified live (130 rows, exact match), merged +105 net new. San Diego County (`agent-06073`, explicit "probe_then_wire" task) was ruled out after live probing both candidates: the primary dataset (`dyzh-7eat`, Building Permits) has real commercial categories but the entire table is stale -- `MAX(issued_date)` across all 236,722 rows is 2023-12-05, over 2.5 years old; the backup (`eqjy-uqyj`, Contractor Trades) is a contractor-license lookup table with no address/date/commercial fields at all, not permit-level data. Neither wired in -- exactly the "mark weak and move on" case the probe instructions anticipated. |
| 68 | `data/states/*.json` moved to git-lfs | Done | 2026-07-28: `nj.json` (63.6MB) and `tx.json` (54.9MB) were already past GitHub's 50MB soft-warning threshold, with several more states (il/fl/oh/nc/ct, 9-30MB) growing at the same rate. Prospective-only migration (Asif's explicit choice over a full `git lfs migrate import` history rewrite, which would have force-pushed and changed every commit SHA on this branch): added `data/states/*.json` to `.gitattributes`, then `git add --renormalize` converted the current working-tree content to LFS pointers in one forward commit. Past commits still store these files as regular git blobs -- repo size doesn't shrink retroactively -- but growth from this commit onward goes through LFS. Confirmed working (269MB of LFS objects uploaded cleanly, no size warnings on push). Transparent to the pipeline scripts -- `merge_into_state()` still just writes plain JSON, `git add`/`commit` handle the LFS conversion automatically. |
| 69 | Dallas County (TX) probed, ruled out -- both candidates stale | Done | 2026-07-28 (`agent-48113`, "probe_then_wire"). Primary (`9qet-qt9e`, Certificates of Occupancy): real commercial-ish fields (`land_use`, `date_issued`) but `MAX(date_issued)` across the whole table is 2022-11-15 -- 3.5 years stale. Backup (`e7gq-4sah`, Building Permits): real fields including `permit_type` with explicit "Commercial" text, but `issued_date` is stored as **text** in `MM/DD/YY` format, not a real date type -- a naive `$order=issued_date DESC` misleadingly topped out at "12/31/19" (string-sorts wrong: e.g. "12/31/19" > "01/01/20" lexicographically). Verified the real cutoff properly via `issued_date LIKE '%/26'` / `'%/25'` (both zero) vs `'%/19'` (52,037, confirming the LIKE pattern works) -- dataset genuinely stops around 2019-2020. Neither wired in. Second real instance this session of a Socrata catalog listing looking promising by field names alone but being dead on live data -- the probe discipline is earning its keep. |
| 70 | San Diego (CA) -- real source found after the county-level Socrata dead ends, new CSV-download provider type | Done | 2026-07-28. Asif ran a Vertex/Gemini query asking for San Diego commercial projects; the response wasn't itself a usable lead (no direct dataset, just generic portal URLs the model admitted it couldn't browse), but it correctly pointed at checking the *city's* open-data infrastructure separately from the *county's* (which was already ruled out as item 67/69's dead ends, both under `internal-sandiegocounty.data.socrata.com`). Followed that up by hand: `data.sandiego.gov/datasets/development-permits/` links to direct CSV downloads at `seshat.datasd.org` -- a completely different, actively-maintained pipeline (verified live: `last-modified` header from *today*, MAX(`APPROVAL_ISSUE_DATE`) = 2026-07-27, 440 real commercial-filtered rows in a bounded 30-day window). Built `core/ingestion/csv_download_provider.py` (first flat-file provider, no query API at all -- filters client-side via include/exclude keyword match on `JOB_BC_CODE_DESCRIPTION` since there's no server-side WHERE clause to push into). Points at the year-specific `approvals_issued_2026_datasd.csv` (15.9MB) rather than the all-years file (590MB, too large to re-download every run) -- needs a URL bump to the 2027 file in January. Also fixed a real generic_mapping.py bug found while verifying: the city-parsing fallback assumed "street, city" (2 comma parts, LA's format) and mis-parsed San Diego's "street, city, ST[ zip]" (3 parts) as city="CA" or "CA 92121" -- now detects a trailing state-abbreviation-like last part and falls back to the second-to-last part instead. Merged +440 net new, CA total now 2,926. Lesson for the "should I still run Flash after deterministic" question this came from: yes, per the established order, but the LLM's own answer isn't the source -- it's a pointer that still needs live verification, same as every other lead this session. |
| 71 | Dallas County (TX) -- gave Gemini a rigid structured-JSON prompt as a test; it hallucinated 100% of the URLs. Real source found by hand, timing bug fixed in accela_provider.py | Done | 2026-07-28. Asif proposed a "Data Harvester Orchestrator" pattern: send Gemini a fixed prompt asking for 6 specific URL types (permit portal, plan-review archive, ArcGIS endpoint, open-data API, filetype:pdf search dorks, attachment deep-link pattern) per county. Ran it once for Dallas as a test. **Every single one of the 7 returned items was fabricated** -- 2 domains didn't resolve at all (`dallas-tx-aca.accela.com`, `data.dallascityhall.com`, the latter paired with a Socrata-4x4-shaped resource id that had every appearance of a real dataset but wasn't), 2 city pages 404'd, and the ArcGIS endpoint (`gis.dallascounty.org`) was real infrastructure but had no permits data on it at all -- just demo/sample layers. Different failure mode than item 70's San Diego lead (which honestly said "I can't browse, check these portals yourself") -- here, a rigid template pushed the model to fill every field with a confident, structurally-plausible, entirely invented answer. **Real source found by hand:** Accela agency code `DALLASTX` (Gemini's guess `dallastx8` was close but wrong), confirmed via a live dropdown inspection rather than trusting a follow-up Gemini suggestion for the exact permit-type label text (`"Commercial New Construction Permit"` / `"Commercial Alteration Addition Permit"` -- close to but not exactly what Gemini guessed). Reused the existing `AccelaProvider` (already proven on Gwinnett/El Paso/San Antonio/Downey/Lancaster) rather than building a new Playwright scraper from scratch as suggested -- split into two state keys (`TX-DALLAS-NEW`, `TX-DALLAS-ALT`) since the provider only supports one exact permit-type-label match per config. **Real bug found and fixed while verifying `TX-DALLAS-ALT`:** on a 100+-result search, `page.wait_for_load_state("networkidle")` returned before the results grid actually finished rendering, so the header-row column-map detection failed on the first read and the provider gave up thinking there were no results -- not a real empty state, a timing race (same class of bug as El Monte/Glendale/Carson's EnerGov timing issues earlier this session). Fixed with a bounded poll-and-retry rather than a longer fixed wait; regression-checked against El Paso and Downey afterward to confirm the fix didn't change behavior for already-working sources. Merged: +46 (new construction) + 251 net new (alteration, 30 overlapping with the first search) = 297 net new. TX total now 44,200. **Takeaway for future county-source discovery:** Gemini's hit rate depends heavily on prompt shape -- an open-ended "what do you know about X" answer is more likely to honestly hedge, while a rigid "fill in this exact schema" prompt pushes it toward confident fabrication. Either way, nothing gets wired in without independent live verification of every URL and every claimed field value. |
| 72 | Bexar County (TX) -- Gemini query with Google Search grounding, real dead end + real gap fix in existing San Antonio source | Done | 2026-07-28. Re-ran the Master Query Template for Bexar County with Vertex's Google Search grounding tool enabled (vs. item 71's bare generation call) -- hit rate improved substantially (6/8 URLs real vs. Dallas's 0/7), though grounding metadata citations didn't come back in the response despite the tool being enabled. Still verified every URL live regardless. Real finding: Bexar County's own permit process (unincorporated areas, outside city limits) is a manual Fire Marshal paper-form review, not a digital portal -- the one real ArcGIS layer Gemini surfaced (`PW/SWQ_Permits`) is stormwater permits only, unrelated to commercial building activity. The real commercial permit volume in Bexar County flows through the City of San Antonio, already wired (`TX-SANANTONIO`) but suspiciously thin at 22 total records for the 7th-largest US city. Investigated and found the real gap: `TX_SANANTONIO_ACCELA_CONFIG` only ever captured 1 of 19 real commercial permit-type categories in the live dropdown (`"Commercial New Building Permit"`). Live-probed the other 18; real 30-day volume: Addition 5, Finish Out 14, Remodel 73, Sitework 37, **Project Application 221** (likely the umbrella application type most projects route through first), Shell 0 (skipped). Added 5 new state keys (`TX-SANANTONIO-ADDITION/FINISHOUT/REMODEL/SITEWORK/PROJAPP`) reusing the same `AccelaProvider`. Merged +330 net new (20 overlap in Project Application with already-captured records, expected). TX total: 44,530. Confirms the same lesson as Dallas from a different angle: even an *already-wired, already-verified* source can have a real, large coverage gap if only one permit-type category out of many was ever checked -- worth periodically re-auditing existing Accela configs' dropdown coverage, not just adding new counties. |
| 73 | TDLR TABS -- first STATEWIDE Texas source, found chasing a Harris County dead end | Done | 2026-07-28. Continued the Harris County investigation through a durable Gemini chat session (`scripts/gemini_discovery_chat.py`, history in `data/gemini_sessions/`) after Gemini's Houston-specific leads (CKAN, GIS hubs, CIVICS permitting portal) all turned out to have no usable public commercial-permit data. Gemini's suggested TDLR (Texas Dept. of Licensing & Regulation) Architectural Barriers registry URL was itself wrong (404), but the underlying pointer was real -- under Texas Administrative Code Ch. 68, commercial construction/renovation projects >= $50k must register with TDLR's TABS system before local permitting, **statewide, covering all 254 counties in one feed**. Verified live: 339,076 total projects on record, 1,913 registered in a real 30-day statewide window, spread realistically across counties (Harris 353, Dallas 226, Travis 132, Bexar 130, ...). Confirmed fully **stateless** -- no cookies/session/CSRF needed at all (a first for this session; every other Playwright-based provider needed a real browser) -- both the county-code lookup table and the search itself work via plain HTTP GET/POST to a standard jQuery DataTables endpoint. Built `core/ingestion/tdlr_tabs_provider.py`, the first provider in this pipeline not scoped to a single county: `TX-TDLR-TABS` pulls all counties in one run, each row's real county attributed from its own numeric code via a live-fetched lookup (not hardcoded). Merged +1,913 net new (zero overlap with existing sources, as expected for a genuinely new registry). TX total: 46,443. **Strategic implication for "keep specindex.ai current at scale":** this is the answer to scaling past one-county-at-a-time Accela/EnerGov hunting -- a handful of real statewide/regional registries (state architectural-barriers systems, state contractor licensing boards, state fire marshal databases) likely exist per state and would each cover dozens of counties in a single feed, the same leverage SAM.gov/USAspending already provide at the federal level. Worth actively searching for TDLR-equivalent statewide sources in other high-value states before continuing further one-county Accela/EnerGov work. |
| 74 | Jacksonville / Duval County, FL -- genuine dead end, ran the full 7-step process | Done | 2026-07-28. Custom-built Angular app (JaxEPICS); the AdvancedSearch page Gemini named turned out to be a saved-searches management feature (itself 504'ing), not permit search -- homepage title is literally "Login". Individual permit detail pages are real and publicly viewable (Google-indexed), but no anonymous bulk/date-range search was found. A feedback-loop follow-up asking about a "Guest Sign In" flow got a confident, specific-sounding answer (a "Permits Plus" logo, a quoted guest-mode banner) that was never actually observed on the live site -- a useful data point that hallucination can persist even after grounding + honest-hedging prompts work well elsewhere. ArcGIS follow-up: maps.coj.net's real 33-folder catalog has nothing permit-related; the one plausible hit (BID/PermitBoundary) is real but token-gated. Real account creation was investigated too (Asif asked "what can I do to get login") -- turned out to require a downloaded paper form + email submission (4 role-specific forms: Contractor/Design Professional/Homeowner/Civil Professional), not self-service signup, and even with an account there's no confidence it would unlock bulk search rather than just permit-application submission -- recommended not pursuing given the friction-to-payoff ratio. Full findings logged to `data/jurisdiction_health_matrix.json` (5 sub-entries). |
| 75 | Colorado Springs / El Paso County, CO -- real CAPTCHA dead end + a working Accela source | Done | 2026-07-28. Three leads from one grounded query, consolidated government structure confirmed correct (PPRBD handles building permits regionally; city/county planning stay separate). **PPRBD** (`pprbd.org/Search/Permit`): real, excellent search form -- `ProjectType` dropdown has clean `New Commercial`/`Commercial Alt`/`Commercial CO` categories, a built-in `Last 30 Days` date range, and a `Jurisdiction` filter covering Colorado Springs + El Paso County + 6 more towns in one form -- but search submission is blocked by a real Cloudflare Turnstile CAPTCHA ("Please complete the security check below to verify you are human"). Not pursued -- same category as this session's Compton dead-end; CAPTCHA/anti-bot-evasion tooling is out of scope regardless of legitimate purpose. **City of Colorado Springs Accela** (`COSPRINGS`): real, not login-gated, only one permit type in the dropdown ("Building Permit Review", no commercial/residential split) -- confirms Gemini's own description that this Accela instance handles Planning/Public Works/Fire/Stormwater, not the primary building-permit channel (that's PPRBD). Verified live anyway: 110 rows/30 days, 105 real ("Commercial building permit." descriptions, real BLDREV-YY-NNNNN case numbers), 5 administrative noise (re-inspection fee records) -- good enough ratio to keep without new content-filtering infrastructure. Added `CO-SPRINGS`, merged +100 net new. **El Paso County EDARP** (`epcdevplanreview.com`): real, but a development/zoning-application system (not building permits) requiring Register/Log in for actual data -- not investigated further given CO-SPRINGS already provided a working source. CO total: 5,341. |
| 76 | City of Cleveland, OH -- clean run, real second Accela source distinct from Cuyahoga County | Done | 2026-07-28. Ran the full 7-step process end to end with no dead ends: discovery (Gemini named agency code `COC` correctly on the first try), live verification, provider wiring, merge, integrity check. Confirmed City of Cleveland's Dept. of Building and Housing runs its own Accela instance, separate from Cuyahoga County's ArcGIS feed already in the corpus (`OH-CUYAHOGA`) -- same LA-City-vs-County / San-Antonio-vs-Bexar pattern recurring again. 98 total permit-type options in the dropdown, 15 real commercial categories; live-probed the 3 most likely ("Commercial Building Construction Permit" 144/30-days -- dominant, kept; "Commercial Combo" 0 -- skipped; "COO - New Commercial" 2 -- small but free, kept). Added `OH-CLEVELAND` + `OH-CLEVELAND-COO`. Merged +126 net new. OH total: 14,182. |
| 77 | 6-month backfill (15 Tier 1 sources) + SAM.gov/USAspending for all 50 states + first real Step 7 win (GA SAM.gov documents to GCS) | Done | 2026-07-28, three-part batch. **Backfill:** re-ran all 15 already-verified deterministic (no-LLM) Socrata/ArcGIS/CARTO sources with lookback_days=180 instead of 30 -- real growth across the board (CA 2,926->5,930, IL 22,478->26,061, FL 21,893->25,902, OH 14,182->16,417, TX 46,443->48,024, WA 3,061->3,634, NC 6,712->6,944, PA 1,193->1,799, MI 1,487->1,702). The 17 sources needing Flash/Sonnet (NJ, NC statewide, 7 TX counties, etc.) deliberately excluded from this batch -- real LLM cost at 6x the window, flagged rather than run silently. **SAM.gov + USAspending, all 50 states:** both providers were already architecturally per-state (SamGovProvider filters one shared 20h-cached national CSV client-side; USASpendingProvider genuinely queries server-side per state, no API key/quota) -- only GA had a registered config. Generated configs for the other 49 states programmatically (not 98 manual blocks) and ran all of them with a 730-day lookback. Corpus total: 230,432 projects across all 50 states. **First real Step 7 win:** SAM.gov's public opportunity API (`sam.gov/api/prod/opps/v3/opportunities/{noticeId}/resources`) genuinely exposes real downloadable attachments -- verified live against a real captured GA project (Robins AFB Bldg 59D renovation): structural drawings, bid abstracts, site-visit sign-in sheets, all `accessStatus: public`, zero auth needed, confirmed by actually downloading and file-type-checking a real 984KB PDF. Built `scripts/fetch-sam-gov-documents.py` -- GCS-only per Asif's explicit instruction, streams straight to `gs://specindex-ai-raw-documents/georgia/{project-id}/{filename}`, never touches local disk. Ran for all 44 GA SAM.gov projects: 30/44 had real public documents, 411 files uploaded, 752MB total. This is the inverse of Cleveland's Accela 403 finding two items up -- confirms document-access claims genuinely vary by source type and must be verified per-source, not assumed uniformly good or bad. |
| 78 | New York City (all 5 boroughs) -- first NY source, wired via 7-step process | Done | 2026-07-28. Discovery/verification found two DOB Socrata datasets: legacy BIS (`ipu4-2q9a`) ruled out stale, MAX(issuance_date)=2020-06-05. DOB NOW: Build Approved Permits (`rbx6-tga4`) confirmed fresh (MAX(issued_date)=2026-07-27, 975,366 rows) but has no building-classification field -- `work_type` is trade category (Plumbing, Sprinklers, General Construction), not occupancy type. Asked Gemini for a cleaner alternative; it named DOB NOW: Build Job Application Filings (`w9ak-ipjd`), verified live: MAX(filing_date)=2026-07-27, 935,285 rows, and it genuinely has `building_type` with real values (Other=702,095, 1 Family=91,101, 2 Family=100,437, 3 Family=24,044) -- `building_type='Other'` is the real commercial/residential split, same pattern as the legacy BIS dataset. Wired as `NY-NYC` (socrata + generic_mapping, no LLM cost), 180-day lookback: 57,804 rows fetched, 57,758 merged into `ny.json` (61,257 total). Corpus integrity check clean across all 51 state files. id prefix `ny-nycdob` chosen to avoid collision with the existing `ny-nyc` prefix (unrelated NYC Capital Projects Database manual-research batch already in the corpus). NYC is New York's top jurisdiction by population by a wide margin (>8M vs. ~950K for Suffolk, the 2nd largest county) -- also satisfies the "top county per state" backlog for NY. |
| 79 | Top-county-per-state backlog started: Cambridge MA (Middlesex) + Minneapolis MN (Hennepin) | Done | 2026-07-29. Asif asked for the most populous county in every state to get 6 months of data via the 7-step process. Both MA and MN have no county-level building-permit function at all (purely municipal in both states) -- had to target the top county's largest city instead. Gemini's first-pass answers for both were partly fabricated: Cambridge's claimed dataset ID (`25q4-7asf`) 404'd (found the real one, `9qm7-wbdc`, via Cambridge's own Socrata catalog search instead); Minneapolis's claimed ArcGIS org ID (`1st35idbL2i24j8I`) returned "Invalid URL" even though the item GUID it gave was correct (found the real org ID, `afSMGVsC7QlRK1kZ`, via a direct ArcGIS Online item-info lookup on that GUID). Both real feeds verified fresh (MAX dates 2026-07-20 and 2026-07-27) with genuine commercial-vs-residential split fields (`proposed_building_use='Commercial / Mixed Use'` for Cambridge, `permitType='Commercial'` for Minneapolis). Wired as `MA-CAMBRIDGE` (3 rows in 180 days -- thin, new-construction-only permits are rare) and `MN-HENNEPIN` (1,787 fetched, 803 new). Also checked Somerville, MA (`nneb-s3f7`) -- real, fresh dataset but genuinely has no property-use/building-type field of any kind, so no reliable commercial filter exists; skipped rather than guess. |
| 80 | Salt Lake City, UT (Salt Lake County) -- Accela agency-code collision caught before it corrupted the corpus | Done | 2026-07-29. Gemini's first answer for Salt Lake City's Accela agency code was `SLC` -- looked plausible (matches the city's initials, same convention as every other working Accela config in this file). Live-probed it before building against it and found `aca-prod.accela.com/SLC` actually resolves to **St. Louis County, Missouri** (error-page links reference `stlouiscountymo.gov`; the module list -- Licenses/N/PublicWorks/WaterandSewer -- has no Building module, inconsistent with a vertical-construction permit portal). Accela's shared multi-tenant hosting means agency codes are global and NOT derivable from a city's initials, unlike every prior Accela config wired so far where the obvious code happened to be right (Gwinnett, El Paso, San Antonio, Dallas). Sent the wrong-agency evidence back to Gemini, got the real code: `SLCREF`, verified live (200 on Welcome.aspx, real Building/Engineering/Fire/Planning modules present). Wired as `UT-SALTLAKE`: 300 rows fetched (180-day window, hits the Accela provider's page cap), 250 new after merge. Lesson for the rest of this backlog: verify every Accela agency code live before trusting an initials-based guess, even a confident-sounding one. |
| 81 | Montgomery County MD (genuine county-level source) + Milwaukee WI (6th platform type gains no-LLM mapping) + Honolulu HI ruled a real dead end | Done | 2026-07-29. **MD:** unlike every other state hit this session, Maryland county government runs permitting directly -- `7ate-xrxm` is pre-filtered to `applicationtype='COMMERCIAL BUILDING'` by the county itself (2,272/2,272), MAX(issueddate)=2026-07-24, Gemini's answer fully accurate this round. **WI:** CKAN is the 6th platform type (after Socrata/ArcGIS/Accela/EnerGov/CARTO/CSV), reusing `ckan_provider.py` first built for Santa Monica CA -- but that provider never actually implemented `to_projects()`/the generic-mapping opt-in (Santa Monica always routed through Flash/Sonnet), so merging Milwaukee crashed with `AttributeError: 'CkanProvider' object has no attribute 'to_projects'`. Fixed by adding the same feed_id/name_fields/etc constructor args + `to_projects()` method already standard on SocrataProvider/ArcGISProvider/CartoProvider/CsvDownloadProvider, and wiring the new kwargs through `factory.py`. Milwaukee's field names contain spaces (`Permit Type`, `Date Issued`) -- ckan_provider.py inserts date_field/watermark_field literally into raw SQL, so the fix was embedding the double-quotes directly in the config string rather than patching the provider. Both MD and WI merged 0 *new* records (368 and 263 fetched respectively) because both counties were already captured by earlier one-off manual research (3,198 and 1,297 pre-existing records) -- good corroboration of that prior work, and both are now wired into the automated pipeline for future incremental refresh. **HI:** Honolulu's only permit dataset (`4vab-c87q`) is a frozen snapshot, verified stale (MAX(issuedate)=2025-07-01, >1yr old). Sent the staleness evidence back to Gemini, which gave an honest "no active public API exists" answer on the retry (DPP's live HNL Build/CivCheck AI system is UI-only, no SODA/REST/OData) -- a real dead end, not a hallucination this time, logged as FAILED_NEED_ALT rather than built against. |
| 82 | St. Louis County MO wired (same "SLC" Accela code as Salt Lake City -- coincidence, two different real agencies); Shelby County TN + Oklahoma County OK deferred | Done | 2026-07-29. **MO:** the default "Building" Accela module 404s for St. Louis County's "SLC" agency (same collision discovered while chasing Salt Lake City earlier this session -- confirmed these are genuinely two different real agencies that happen to share a 3-letter code, not the same mistake twice). Fetched the "PublicWorks" module's real search-form dropdown HTML and found vertical commercial permits live there as record type `PublicWorks/Building/Commercial/New Building` (label "BUILDING COMMERCIAL NEW BUILDING"). Wired as `MO-STLOUIS`: 190 fetched, 180 new. **TN:** Gemini gave two fabricated/dead domains in a row for Shelby County/Memphis -- `datamidsouth.org` (DNS failure) and, on retry, `aca.memphistn.gov` (also DNS failure). The real Memphis open-data portal (`data.memphistn.gov`) exists but is an ArcGIS Hub with no permit FeatureServer; the real upstream system (Develop901, Accela-based) is confirmed to exist but its real agency code hasn't been found yet -- deferred rather than guessed, worth a fresh attempt using the same live-search method that found `SLCREF` for Salt Lake City. **OK:** confirmed no public API exists for OKC/Oklahoma County permits (data.okc.gov explicitly excludes building permits from its catalog); OKC's Accela agency code also not yet found -- deferred for the same reason. |
| 83 | Fulton County GA (fills GA's real top-county gap) + Jefferson County KY (Louisville Metro) + Multnomah County OR (Portland) | Done | 2026-07-29. **GA:** existing coverage (`GA_GWINNETT_ACCELA_CONFIG`) was GA's #2 county by population, not #1 -- Fulton is #1 but the county government itself issues almost no permits (nearly 100% incorporated into Atlanta/Sandy Springs/Roswell/etc), so wired City of Atlanta instead. Accela agency code `ATLANTA_GA` verified live, real Building module has 20+ Commercial-prefixed record types; wired "Commercial New". 91 fetched, 81 new. **KY:** Louisville Metro is a consolidated city-county government, so this is genuine full-county coverage, not a top-city proxy. Gemini's answer was unusually accurate this round (dataset name and the full field schema -- `PERMIT_NUMBER`/`CATEGORY_NAME`/`WORK_TYPE`/etc -- matched exactly on live verification) but didn't supply the real ArcGIS service URL, found via a direct item search + item-info lookup. `CATEGORY_NAME='Commercial'` real (888 rows), MAX(ISSUE_DATE)=2026-07-28 -- the same day as this verification, the freshest source found all session. 77 fetched, 77 new. **OR:** Portland Permitting & Development does not use Accela at all -- publishes directly via PortlandMaps' own ArcGIS REST services. The layer is literally named "Commercial Construction Permit", pre-filtered by the city itself (same no-where-clause-needed pattern as MD-MONTGOMERY). 75 fetched, only 1 new -- most of this source's activity was already captured by earlier manual research, same corroboration pattern as MD/WI. |
| 84 | Fairfield County CT ruled a real dead end; Bernalillo County NM wired via Accela fallback; Jefferson County AL deferred | Done | 2026-07-29. **CT:** genuine dead end -- CT abolished county government in 1960, checked every major Fairfield County municipality (Stamford, Bridgeport, Norwalk, Fairfield town, Greenwich), all use proprietary platforms (OpenGov/CitySquared) with zero public API, no Accela anywhere in the county. Honest Gemini answer, no fabrication this round. **NM:** Gemini's primary lead (Albuquerque's own `gis.cabq.gov` ArcGIS service) is unreachable -- DNS resolves fine but the TCP connection to port 443 times out even on a 25s retry, a real infrastructure dead end rather than a hallucination. Fell back to its secondary lead, Bernalillo County's own Accela portal (agency `bernco`), which has a real "Commercial Building" record type (`BCBP` code) -- matches what Gemini separately claimed for a monthly bulk CSV export, good corroboration. 122 fetched, 82 new. **AL:** two real Accela agencies confirmed reachable (`BIRMINGHAM` city, `JCCAL` unincorporated county), but Birmingham's Building module only exposes a generic "Building Permit" label with no visible Commercial/Residential split (possibly a second-level cascading dropdown not yet investigated), and JCCAL's Building module page loads but returns zero dropdown options and no title -- needs a different module param or further diagnosis. Deferred rather than wire an unfiltered label that would pollute the corpus with residential permits. |
| 85 | Douglas County NE (Omaha) wired -- confirms a real pattern: Accela's default "Building" module is wrong about as often as it's right | Done | 2026-07-29. Agency code `OMAHA` verified live, but the default `module="Building"` returned a 200 with zero dropdown options -- the exact same empty-page symptom already seen for MO-STLOUIS (`SLC`) and the still-deferred AL-JCCAL. Checked OMAHA's real module list via `Welcome.aspx` and found there's no "Building" module at all: the real one is "Permits" (alongside Enforcement/Fire/Licenses/Planning/PublicWorks/Rentals). Real record type `Permits/BUILDING/COMMERCIAL/NEW BUILDING` (label "New Building") confirmed live. Wired as `NE-DOUGLAS`: 121 fetched, 111 new. This is now the third time this exact failure mode has shown up in this session's Accela work -- worth checking an agency's real module list *before* assuming "Building" on every future Accela source, not just when the default 404s or comes back empty. Also worked RI (Providence) and KS (Johnson County/Olathe) this round -- both hit dead leads (an unreachable ArcGIS host for RI, a wrong service path for KS) and were deferred rather than pushed further, given diminishing returns on this pass. |
| 86 | East Baton Rouge Parish LA + Greenville County SC wired; Polk County IA deferred; new arcgis_provider.py date-literal style added | Done | 2026-07-29. **LA:** genuine parish-level source, not a top-city proxy -- `data.brla.gov` is the consolidated City-Parish government's own portal, Gemini's answer fully accurate on the first try. `designation='Commercial'` real (46,656/142,426), fresh (MAX=2026-07-24). 1,092 fetched, 1,091 new -- one of the largest single-run yields this backlog. **SC:** wired City of Greenville (county itself has no API, only an eTRAKiT web portal); found the real ArcGIS service URL via an owner-scoped item search after Gemini gave only the layer name. Hit a genuine ArcGIS Server bug mid-verification: this old server (version 10.81) returns "Failed to execute query" on *every* DATE/TIMESTAMP literal style tested against the real date field (`NewIssueDate`), while a plain `PERMIT_TYPE=` filter works fine -- not a syntax problem, a server-side one. Worked around it using `APPLICDATE`, a second field on the same layer that stores the date as a plain `YYYYMMDD` number (e.g. `20260406.0`), queryable with a bare numeric comparison -- added a new `"yyyymmdd_int"` `date_literal_style` option to `arcgis_provider.py` for this (joining `"date"`/`"timestamp"`/`date_field_is_string` as the 4th literal-format variant this file has needed across different ArcGIS Server deployments). `PERMIT_TYPE='BLDC'` confirmed real, matching Gemini's claimed code exactly. 77 fetched, 77 new. **IA:** Polk County/Des Moines confirmed no public API by Gemini (OpenGov county side, Tyler EnerGov city side) -- deferred rather than dug into further, though worth a direct EnerGov subdomain check later since other EnerGov instances (Carson CA) turned out to have real public search APIs this session despite similar upfront "no API" claims. |
| 87 | Ada County ID (Boise) wired via a non-standard Accela domain; Pulaski AR and Hinds MS both dead ends | Done | 2026-07-29. **ID:** the standard `aca-prod.accela.com/boise` URL 301-redirects -- Boise runs its Accela instance on its own branded domain, `permits.cityofboise.org/CitizenAccess`, found by simply following the redirect rather than assuming the standard URL pattern always holds. Real Building module dropdown option "502-New or Added Commercial" confirmed live. 118 fetched, 98 new. **AR:** Gemini's Little Rock dataset ID (`waw2-4wxz`) 404'd, and `data.littlerock.gov` itself redirects to a plain informational page -- there's no Socrata portal there at all, a dead domain rather than just a wrong ID. Deferred rather than guess further. **MS:** genuine dead end for Hinds County/Jackson -- no open-data platform of any kind, Accela is portal-only with no public API, honest Gemini answer with no fabrication. |
| 88 | New Castle DE, Hillsborough NH, and Yellowstone MT investigated -- all deferred, no new sources wired | Done | 2026-07-29. **DE:** genuine dead end, county uses ProjectDox/ePlans with a web-form-only portal, no API of any kind. **NH:** genuine dead end, NH counties have zero permitting function by law, Manchester is fully paper/PDF-based, Nashua's ArcGIS Hub has parcels only. Gemini correctly flagged its own risk of confusing this with Hillsborough County FL (a real Accela/HillsGovHub jurisdiction) rather than conflating them. **MT:** found a real statewide Accela agency (`MONTANA`, Montana DLI Building Codes Bureau, verified live) but it only covers projects *outside* municipal enforcement -- i.e., explicitly not Billings, the county's actual commercial center -- and its Building module has just one generic "Building Permit" catch-all with no commercial/residential split. Deferred rather than wire a feed that's both low-relevance and unfilterable. Three states in a row with no new source -- a reminder that population-ranked prioritization has diminishing returns past the top ~30-35 counties, consistent with the ~0.3%-of-counties finding already noted in `docs/AGENT_STRATEGY.md`. |
| 89 | IA/RI/KS retried with specific failure evidence -- IA and RI now confirmed genuine dead ends, KS found a real but administratively-stopped service | Done | 2026-07-29. Asif asked to retry the 3 deferred states where the first pass hit a wrong URL/path rather than a clear "no API" answer. **IA:** real EnerGov subdomain found (`css.dmgov.org`, not the guessed `selfservice.dsm.city`) but it's a closed SPA with session/CSRF-protected internal endpoints; Polk County's OpenGov portal is the same story. Confirmed dead end. **RI:** real current GIS Hub found (`pvd-gis-providenceri.hub.arcgis.com`, replacing the unreachable `webgis.providenceri.gov`) but has no permit layer at all; Socrata dataset confirmed frozen at 2018-09-18. Confirmed dead end. **KS:** found a real, well-identified, well-named service -- `maps.jocogov.org/arcgis/rest/services/Permits/MapServer/0`, Johnson County AIMS, explicitly countywide (aggregates permits across all cities, higher value than a single-city proxy) -- but it returns `{"code":500,"message":"Service Permits/MapServer not started"}` on two separate checks a few seconds apart, an admin-stopped state rather than a cold-start delay. Left as FAILED_NEED_ALT rather than NO_PUBLIC_DATA since this is a real, specific, identifiable service worth periodically re-checking, unlike IA/RI's genuine platform-level dead ends. Cleaned up `data/raw/` in the same batch -- deleted ~44MB of untracked, superseded scratch files (NJ document-classification comparison runs, GA/NC/TX county-source research dumps, TX Collin/Travis raw pulls superseded by the later statewide TDLR TABS source). |
| 90 | Sioux Falls SD + Indianapolis IN wired; ND/VT/WY confirmed dead ends; real Accela ID-collision bug caught and fixed | Done | 2026-07-29. Finished the top-county-per-state backlog's last 5 states. **ND, VT, WY:** all three confirmed genuine dead ends -- no Accela/Socrata/CKAN/CARTO/ArcGIS permit layer anywhere in any of the three, only closed vendor portals (OpenGov, SmartGov) or web-form-only city systems. **SD:** Gemini's domain had a `.org`/`.gov` typo (`gis.siouxfalls.org` doesn't resolve; real is `gis.siouxfalls.gov`), found via an ArcGIS Online item search on the same item ID embedded in Gemini's own GeoJSON download URL. `PERMITTYPE='Commercial Building'` real, fresh (MAX=2026-07-25). 1,368 new -- one of the largest single-run yields in the whole backlog, from one of the smallest states. **IN:** resolves Marion County's "flagged hard" status from an earlier session, which assumed the only option was Indiana's statewide Oracle Reports CDR system -- Indianapolis actually runs its own Accela instance (`INDY`, reached via the same branded-domain redirect pattern as Boise ID), and uses "Improvement Location Permit" terminology instead of "Commercial" (real type: "Improvement Location Permit-Non-Residential"). **Caught a real data-corruption bug during verification:** Indianapolis's results table uses "Case Number" as its permit-ID column header, which wasn't in `accela_provider.py`'s `HEADER_ALIASES` map -- every one of 326 real rows got the identical fallback ID (`in-marion-accela-marion`) and silently collapsed into a single record on the first merge. Caught immediately (checked unique-ID count on the pipeline output before trusting the merge result), fixed the alias list, deleted the 1 corrupted record, reset the watermark, and re-ran clean: 281 new of 341 fetched. Worth noting for any future Accela source: always sanity-check unique ID count on a first run, not just the row count, since a column-mapping miss fails silently rather than erroring. This closes out the top-county-per-state backlog for all 50 states: 17 states wired this session, 16 confirmed dead ends/deferred with reasons logged in `jurisdiction_health_matrix.json`, the remaining ~17 states already had top-county coverage from earlier work (see item 78 and [[project_specindex_top_county_per_state]] for the full state-by-state breakdown). |
| 91 | Step 7 document pulls checked for all 17 sources wired this session -- confirmed no new wins, honest dead end rather than forced | Done | 2026-07-29. Checked whether any of this session's 17 newly-wired sources could support a document pull. The 10 ArcGIS/Socrata/CKAN sources (NY, LA, MN, KY, OR, SC, MD, WI, MA, SD) are open-data *exports* of permit records -- their schemas were already fully inspected while wiring each one, and none carry a document/attachment field at all, so there's nothing to pull from the API layer itself. The 7 Accela sources (UT, MO, ID, NE, GA-Atlanta, NM, IN) each have a case-management "Attachments" tab, so live-verified two of them with a real Playwright check (reusing the same browser-automation approach as `accela_provider.py`, since raw curl can't drive Accela's ASP.NET postback forms) rather than assuming: UT-SALTLAKE and IN-INDIANAPOLIS both show the identical pattern -- an upload-only form gated to "Users with Permissions to Upload Documents," the same standard Accela `FileUpload/AttachmentsList.aspx` widget, no public document listing at all. This exactly matches the earlier Cleveland OH finding (a different Accela deployment, same conclusion) -- three independent agencies now confirm this is standard Accela "General Public" access-level behavior, not a per-agency fluke, so the remaining 5 Accela sources weren't individually re-tested. Net result: zero new step-7 wins this round. GA-SAM.gov remains the only real step-7 success to date -- federal solicitation attachments are public by law, municipal/county case-management attachments generally are not. |
| 92 | Homepage rebuild (specindex_sample.html + build brief) surfaced 4 unbuilt integrations -- logged as roadmap items, not shipped as live | Not started (P2) | 2026-07-31. Asif provided a Claude-Projects-designed landing page mockup + build brief for a homepage rebuild aimed at early-access recruitment and fundraising credibility. Brief's "Delivered to your stack" section listed Snowflake, Databricks, MCP server, REST API, and CSV/Excel export as live integrations. Verified against the actual codebase before shipping any of it (brief's own rule: "only list what is actually real"): REST API is real (`api_keys` table, `X-Api-Key` header auth in `api/main.py`). The other 4 are not built anywhere in the repo -- no MCP server exists at all, no Snowflake/Databricks connector code, no CSV/Excel export endpoint. Marked all 4 "coming" on the rebuilt homepage rather than presenting them as live, per Asif's explicit instruction. Real remaining scope, roughly in build-effort order: (1) CSV/Excel export -- straightforward, an export endpoint or client-side conversion off the existing REST API; (2) MCP server -- wrap the existing REST API's project-query surface in an MCP tool definition; (3) Snowflake/Databricks -- warehouse share or scheduled export pipeline, meaningfully more infra than the other two. |
| 93 | Lee County FL date-column gap investigated (no fix found, real remaining scope); Pasco County FL resolved (VERIFIED_WORKING, thin real yield) | Partial (P1) | 2026-07-31. Two known bugs from tonight's batch-2 coverage push, worked in P0/P1/P2 priority order per Asif's ask to track done-vs-pending that way going forward. **Lee County FL (P1, not resolved):** live-checked whether Accela's results grid supports a "customize columns" UI to add the missing Date column that was silently causing 0-row merges -- no real clickable customize control exists on this deployment (the string "customize" only appears in inert page JS). Getting a per-record date here needs individual detail-page fetches, a meaningfully bigger lift than a config fix -- deferred, real remaining scope. **Pasco County FL (P1, resolved):** both previously-tried permit-type labels confirmed dead (one genuinely 0-row, the other 100+ results but live-verified stale at newest-record 04/18/2024). Live dropdown probe found "COM - Interior Build Out With no Change to Existing Slab-Remodel" -- confirmed fresh (newest record 06/10/2026 same-day-adjacent). Dry-run confirmed 1 real row in a 90-day window (Pasco's commercial volume in this subtype is genuinely low, not a config error), merged for real: `data/states/fl.json` 26400 -> 26401. Status flipped FAILED_NEED_ALT -> VERIFIED_WORKING in `jurisdiction_health_matrix.json`. |
| 94 | PA batch-2: individually re-verified Chester/Lancaster/York/Berks/Dauphin (previously only batch-logged as dead ends) -- found one real live source (Reading/Berks EnerGov) that still doesn't clear the merge bar, all others confirmed genuine dead ends | Not merged (P1, real remaining scope) | 2026-07-31. Per the standing PA structural fact (permits are municipal, not county, under the PA Uniform Construction Code), identified each county's largest municipality via Gemini search-grounding and live-probed its actual permit system, rather than re-batch-logging the same county-level conclusion. **Chester:** largest municipality is Tredyffrin Township (~32K, not West Chester Borough as commonly assumed) -- uses Clariti, page loads but not confirmed as a public query database; West Chester Borough's SmartGov portal could not be live-verified at all (see SmartGov note below). **Lancaster:** City of Lancaster confirmed -- CityView Plans Drop is submission-only, no public search, `cityoflancasterpa.gov/permits-licensing/` 404s. Dead end confirmed at city level. **York:** City of York confirmed -- OpenGov/ViewPoint Cloud portal loads (200) but is an Angular SPA shell; WebFetch couldn't confirm a public records-search feature (same "application tracking, not public database" pattern seen elsewhere in this corpus for OpenGov). Not probed with a full Playwright network capture this round (budget) -- real remaining work if revisited. **Dauphin:** largest municipality is actually Lower Paxton Township (~55.6K), not the City of Harrisburg (~50K, 2nd-largest despite being the capital) -- Harrisburg's Bureau of Codes is confirmed paper-only; Lower Paxton's SmartGov portal hit the same SmartGov block as Chester. **SmartGov (Chester + Dauphin):** `smartgovcommunity.com` TLS-handshake-fails (`SSL_ERROR_SYSCALL`) on every request from this environment, root domain included -- consistent with bot-defense/TLS-fingerprinting rather than the site being down. Genuinely unverified, not ruled dead -- worth a retry from Playwright/a different network path rather than bare curl. **Berks -- the one real finding:** City of Reading (~95K, PA's 4th-largest city) runs a live Tyler EnerGov Citizen Self Service tenant (`readingpa-energovweb.tylerhost.net`) with a real, working, unauthenticated search API -- confirmed via actual Playwright network capture of `POST .../api/energov/search/search` returning genuine case records (real CaseNumbers, real Reading street addresses, real ApplyDates). This is structurally different from every other PA county checked tonight: a genuinely live, queryable municipal system, not a dead end. But it doesn't clear the "\>0 real rows on dry-run" bar yet -- the default/blank search's pagination surfaces oldest-first legacy records (2004-2006 permits filled the first 3 pages of the default search even at `lookback_days=900`), the same known class of issue already documented for CA's El Monte EnerGov tenant (`core/ingestion/energov_provider.py`'s docstring). Tried a keyword search (matched on address text, not year) and a hand-crafted sorted API POST (`SortBy=ApplyDate`, `SortAscending=false`) to reach current records directly -- the hand-crafted POST 500'd, the same unexplained "only a genuine Angular-originated XHR succeeds" behavior already documented for El Monte. Not wired into `state_configs.py` since it produces 0 usable rows as-is. All 5 counties' `jurisdiction_health_matrix.json` entries updated in place (still `FAILED_NEED_ALT`) with this individual-verification evidence, replacing the prior batch-only notes. Real remaining work, roughly in priority order: (1) find a working sort/date-filter path for Reading's EnerGov tenant -- same open problem as El Monte, solving it once likely unlocks both; (2) verify SmartGov (Chester + Dauphin) from an environment without the TLS block; (3) a full Playwright probe of York's OpenGov portal rather than the SPA-blind WebFetch check done this round. |
| 97 | LA batch 1 priority-county coverage push (Jefferson/Caddo/Lafayette/Calcasieu/Ouachita) -- no new structured merges, all 5 genuine dead ends, 2 counties document-research-fallback'd | Partial (P1) | 2026-07-31. Started LA's post-EBR priority list. **Jefferson/Caddo/Lafayette:** all three route through MGO Connect (MyGovernmentOnline, api.mgoconnect.org), the dominant LA parish/city permitting platform -- confirmed live via Playwright network capture that Guest browsing works for jurisdiction landing pages but the actual "Search Permits" action redirects to `/cp/login` with no permit-record API call firing beforehand, i.e. genuinely login-gated for anonymous users, not just a UI dead link. This is a real statewide pattern, not three unrelated dead ends -- the same MGO Connect jurisdiction picker also lists Rapides, St. Tammany, Tangipahoa, and Terrebonne, so those are likely to hit the identical gate if checked next. Jefferson Parish's own GIS (`geoportal.jeffparish.net/arcgis/rest/services`) 500'd on the REST root; Lafayette Consolidated Government's GIS host (`lcgmaps.lafayettela.gov`) failed DNS resolution entirely -- neither was a working ArcGIS feed despite Gemini's first answer. **Calcasieu:** Gemini's first URL (`calcasieuparishla-energov.tylerhost.net`) doesn't resolve (legacy Tyler EnerGov CSS decommissioned per Gemini's follow-up); the current replacement (`www.calcasieu.gov/civicaccessportal`) is real but sits behind Akamai Bot Manager, returning a clean 403 Access Denied to both curl and a full headless-Chromium Playwright load -- a genuine bot-block hard stop, not a broken link, same class as Colorado Springs' PPRBD CAPTCHA. **Ouachita:** Ouachita Parish Police Jury's permit page loads (200) but is a plain navigation hub with no search tool or feed at all -- genuinely no digitized system, distinct from a blocked one; City of Monroe is MGO Connect (same gate as above). Ran `document-research-fallback.py` (Part 2) for the two largest counties: Jefferson (4 projects captured, 0 documents confirmed) and Caddo (5 projects captured, 0 documents confirmed) -- document capture is a bonus not a gate, all projects retained regardless. Remaining unworked from the assigned list: Rapides, Livingston, St. Tammany, Bossier, Ascension, Tangipahoa, Terrebonne, Lafourche -- worth noting the MGO Connect login-gate finding before re-investigating each individually, since several are very likely to hit the same wall. |
| 96 | Check GCP billing export cost breakdown for specindex-ai (Gemini/Vertex AI vs. other services) | Not started (P2) | 2026-07-31. Asif asked to understand how costly the specindex GCP project is. No BigQuery billing export existed yet -- created dataset `specindex-ai:billing_export` via `bq mk` and Asif enabled BigQuery export in Console (Billing account "Account 2" -> Billing export -> BigQuery export, project `specindex-ai`, dataset `billing_export`). Export is not retroactive and the export table itself typically takes a few hours to appear, then up to ~24h for first day's data. Two existing budget alerts found on the account: `specindex-ai` project budget $100/month (50/90/100% thresholds), account-wide $200/month (50/90/100/150% thresholds) -- no actual spend-by-service data available from either, they're alert configs only. Faster path for a same-day number: Console -> Billing -> Cost management -> Reports/Cost breakdown for billing account "Account 2", which shows live spend by project/service/SKU without needing the export. Follow-up: once `billing_export` has data, query it (or the Console breakdown) to see what fraction of spend is Vertex AI/Gemini vs. other GCP services -- this was originally prompted by comparing Gemini-for-search costs against a third party's (Revere/Mike Ensing) per-model LLM pricing tiers. |

---

| 95 | SC batch 2 priority-county coverage push (Horry/Spartanburg/Lexington/York/Anderson/Berkeley/Beaufort) -- no new structured merges, 1 genuine near-miss, 7 counties document-research-fallback'd | Partial (P1) | 2026-07-31. Continued SC coverage past Greenville (VERIFIED_WORKING)/Richland (dead end). **Horry:** EnerGov CSS (egweb.horrycounty.org) confirmed live but SSO-gated, same dead-end class as Richland/Columbia; City of Conway (BS&A Online) and Myrtle Beach (no portal) checked as fallbacks, neither usable (BS&A isn't a supported provider_type). **Spartanburg:** real near-miss -- Tyler EnerGov CSS confirmed live and NOT login-gated (unlike Richland/Horry), found and fixed a genuine timing bug in the shared `energov_provider.py` (300ms->1000ms wait after `#SearchModule` change was giving a false 0-row null-Result response on this tenant specifically). Config registered (`SC_SPARTANBURG_ENERGOV_CONFIG`/`SC-SPARTANBURG`) but not yet mergeable: this tenant default-sorts by `PermitNumber.keyword` ascending with no date filter applied, so the 5-page cap only ever reaches case numbers starting with "A" (Addressing/Appeal), never real Building permits -- needs a follow-up fix (SortBy=IssueDate or ApplyDateFrom/To criteria, both visible in the request schema) before merging. **Beaufort:** also a real near-miss -- EnerGov CSS confirmed live, not login-gated, genuinely large dataset (`PermitsFound: 210955` on a blank search), but selecting the Permit module server-errors ("Specified argument was out of the range of valid values") and the default blank All-module search returns `EntityResults: []` despite `TotalFound: 439070` -- needs the right minimal keyword/criteria for this tenant, not found in the time available. **Lexington** (BluePrince/CityView), **York** (Evolve/InfoVision, Gemini's URL 404s), **Anderson** (OpenGov/ViewPoint Cloud), **Berkeley** (reported EnerGov URL 403s, real tylerhost.net tenant hostname not discoverable, Goose Creek city fallback doesn't resolve) all confirmed real platforms but none of the 8 supported `provider_type`s -- structured dead ends. All 7 counties (Horry/Spartanburg/Lexington/York/Anderson/Berkeley/Beaufort) ran `document-research-fallback.py` per Part 2 of the standing process: 34 total projects captured across all 7 (Horry 5, Spartanburg 7, Lexington 5, York 4, Anderson 4, Berkeley 5, Beaufort 4), 0 documents confirmed in this pass -- document capture is a bonus not a gate, all projects retained regardless. Remaining unworked from the assigned list: Pickens, Sumter, Darlington, Kershaw, Lancaster, Chesterfield. |

**Why this file exists instead of only a Google Sheet:** the Drive MCP tools (and the
`.gsheet` stub files Google Drive Desktop syncs locally) have no in-place update
capability for Google-native Sheets/Docs — only create-new and read. Every prior
"update" to the Drive roadmap created a new file instead of editing the existing one,
producing duplicates. This file is the fix: a real file, edited in place, no duplication.
